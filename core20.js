/* Optional schema-2 extension. Opening the app never creates or rewrites records. */
(function(){'use strict';const C=typeof module!=='undefined'?require('./core18.js'):Core;
const previous=C.validateStore;
C.validateStore=function(s){previous(s);if(s.uxV2!==undefined){const x=s.uxV2;if(!x||x.version!==1)throw Error('Unsupported UX V2 record version.');for(const kind of ['activity','notes','equipment']){if(x[kind]!==undefined&&!Array.isArray(x[kind]))throw Error('Invalid '+kind+' records.');const seen=new Set();for(const r of x[kind]||[]){if(!r||!/^[A-Za-z0-9_-]{1,150}$/.test(r.id||'')||seen.has(r.id))throw Error('Invalid '+kind+' identifier.');seen.add(r.id);if(!Number.isFinite(Date.parse(r.at)))throw Error('Invalid record date.');if(kind!=='equipment'&&Date.parse(r.at)>Date.now()+60000)throw Error('Recorded date is in the future.');if(kind==='notes'&&!(typeof r.note==='string'&&r.note.trim()))throw Error('Enter a note.');if(kind==='activity'){if(!r.title?.trim())throw Error('Enter an activity.');for(const k of ['minutes','steps'])if(r[k]!=null&&(!Number.isFinite(r[k])||r[k]<0))throw Error('Invalid activity value.');if(r.steps!=null&&!Number.isInteger(r.steps))throw Error('Steps must be a whole number.');}if(kind==='equipment'){if(!r.name?.trim()||!['syringe','needle','swab','other'].includes(r.type))throw Error('Invalid equipment.');for(const k of ['quantity','low'])if(!Number.isInteger(r[k])||r[k]<0)throw Error('Equipment counts must be nonnegative whole numbers.');}}}}if(s.profile?.targetFat!=null&&(!Number.isFinite(s.profile.targetFat)||s.profile.targetFat<=0||s.profile.targetFat>=100))throw Error('Target body fat must be between 0 and 100%.');return s;};
if(typeof module!=='undefined')module.exports=C;
})();
