const assert=require('node:assert/strict'),C=require('../app/src/main/assets/core16.js');
const rows=[{at:'2026-09-01T10:00:00',values:{weight:90}},{at:'2026-09-01T18:00:00',values:{weight:92}}];assert.equal(C.weightDays(rows,'average')[0].weight,91);assert.equal(C.weightDays(rows,'latest')[0].weight,92);assert.equal(C.selectedWeights(rows,'latest')[0].values.weight,92);
const entries=[{id:'a',at:'2026-09-01T10:00:00',mg:1},{id:'b',at:'2026-09-07T10:00:00',mg:2},{id:'c',at:'2026-09-08T10:00:00',mg:1.5},{id:'d',at:'2026-09-08T10:00:00',mg:4,status:'planned'}];const weeks=C.weekBuckets(entries,Date.parse('2026-09-18'));assert.equal(weeks.length,3);assert.equal(weeks[0].total,3);assert.equal(weeks[1].total,1.5);assert.equal(weeks[2].total,0);
const p={age:52,height:173,weight:88,target:80,activity:1.375,deficit:15,sex:'male',macroMode:'auto',resistance:true};const a=C.macros(p),b=C.macros({...p,keto:true});assert.equal(a.protein,b.protein);assert.ok(b.carbs<=30);assert.ok(Math.abs(a.proteinPct+a.carbPct+a.fatPct-100)<1e-6);assert.ok(a.min<a.max);assert.equal(C.macros({...p,proteinBasis:'target'}).reference,80);assert.throws(()=>C.macros({...p,proteinBasis:'target',target:null}));assert.equal(C.macros({...p,macroMode:'custom',protein:140,carbs:0}).carbs,0);
for(const bg of ['#fff','#ffffff','#091724','#ffee44','#44ccff','#96ff40'])assert.ok(C.contrast(bg,C.inkFor(bg))>=4.5);
console.log('1.6 core checks: weight selection, anchored weeks, nutrition energy/ranges, keto protein parity, contrast.');
